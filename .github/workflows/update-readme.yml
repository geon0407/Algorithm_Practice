name: Update problem index
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: readme-index-${{ github.ref }}
  cancel-in-progress: false
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Generate and commit index
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          for attempt in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            python scripts/update_readme.py
            git add README.md
            for platform in Programmers 프로그래머스 SWEA 백준 BOJ; do
              if [ -d "$platform" ]; then
                git add -- "$platform/README.md"
                for index in "$platform"/*/README.md; do
                  if [ -f "$index" ]; then git add -- "$index"; fi
                done
              fi
            done
            if git diff --cached --quiet; then
              exit 0
            fi
            git commit -m 'docs: update problem index'
            if git push origin HEAD:main; then
              exit 0
            fi
          done
          echo 'Push failed after five attempts.' >&2
          exit 1
